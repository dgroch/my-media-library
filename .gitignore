# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files (secrets — never commit)
.env
.env*.local

# typescript
*.tsbuildinfo

# Note: the generated index lives at src/data/asset-index.json (a committed
# placeholder that `npm run build:index` overwrites). It is intentionally NOT
# ignored so it can be bundled into the build via import. Avoid committing the
# populated (large) version.
